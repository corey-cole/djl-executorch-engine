name: warm-build-image

# Populate the GitHub Actions layer cache for the per-platform toolchain image on the DEFAULT
# branch. GHA scopes caches per ref and lets a run read only its own ref plus the default branch,
# so a PR run can never read a cache another PR wrote. Warming here on `main` is the one scope
# every run CAN read, which turns the image build in native-build-job.yml into a cache hit instead
# of a dnf round-trip on every build. Ported from measly-java-learning/iree-runtime-dist.
on:
  push:
    branches: [main]
    # Only re-warm when the image or how it is named/selected actually changes. A warm on unrelated
    # commits would just re-push identical layers.
    paths:
      - 'docker/**'
      - '.github/workflows/warm-build-image.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  # Single source of truth for the linux build platforms. The aarch64 matrix row in
  # native-build-job.yml is live and runs on ubuntu-24.04-arm; warming its image here is what keeps
  # that job's image build a cache hit instead of a dnf round-trip.
  PLATFORMS: >-
    [
        {"platform":"linux-x86_64","container":"djl-executorch-engine-build:linux-x86_64","runner":"ubuntu-latest"},
        {"platform":"linux-aarch64","container":"djl-executorch-engine-build:linux-aarch64","runner":"ubuntu-24.04-arm"}
    ]

jobs:
  setup:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    outputs:
      platforms: ${{ steps.platforms.outputs.platforms }}
    steps:
        # `tee -a` used in place of `>>` to simplify debugging
      - id: platforms
        run: |
          {
            echo "platforms<<EOF"
            echo "$PLATFORMS"
            echo "EOF"
          } | tee -a "$GITHUB_OUTPUT"

  warm:
    needs: setup
    runs-on: ${{ matrix.combo.runner }}
    strategy:
      matrix:
        combo: ${{ fromJson(needs.setup.outputs.platforms) }}
    steps:
      - uses: actions/checkout@v7
      # Each row runs on a runner of its own architecture (ubuntu-24.04-arm for aarch64), so the
      # image builds natively and no qemu/binfmt setup is needed.
      - uses: docker/setup-buildx-action@v4.2.0
      - name: Warm the pinned toolchain image cache
        uses: docker/build-push-action@v7.3.0
        # No `load:` -- this job only populates the cache; it never runs the image. cache-to writes
        # the layers the build runs read via cache-from.
        with:
          context: docker
          file: "docker/${{ matrix.combo.platform }}.Dockerfile"
          tags: ${{ matrix.combo.container }}
          # Scope per platform, or both rows share the default `buildkit` scope and one arch's
          # cache index shadows the other's (see #38). The scope string
          # MUST match the `${{ matrix.platform }}` scope in native-build-job.yml — editing the two
          # together keeps the warm job populating what the build job reads.
          cache-from: type=gha,scope=${{ matrix.combo.platform }}
          cache-to: type=gha,mode=max,scope=${{ matrix.combo.platform }}
